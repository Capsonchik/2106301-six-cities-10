import { OffersReview } from '../../types/offers';

type mapProps = {
  offers: OffersReview[];
};

function Map({offers}:mapProps) {
  return (
    <div>
      test
    </div>
  );
}

export default Map;
